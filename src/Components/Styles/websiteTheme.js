import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#E9E9E9',
      typography: '#767676'
    },
    secondary: {
      main: '#C6D6FF',
    },
  },
});

export default theme